#ifndef INSERT_UTILS_H
#define INSERT_UTILS_H

// #include "sqlite3.h"

// extern VdbeCursor *OpenWriteTable(VdbeCursor *pCur, sqlite3 *db, int ndp_Db, int ndp_rootpage, int cur_name, int n_cols);
// extern i64 GetNewRowid(VdbeCursor *pC);
// extern int InsertRecord(sqlite3 *db, i64 v, Mem *pData, VdbeCursor *pC);
// extern int AddRawRecord(sqlite3 *db, void **pData, int ndp_Db, int ndp_rootpage, int n_cols);

#endif /*INSERT_UTILS_H*/